{
  "name": "serialize",
  "version": "0.2.0",
  "description": "Utility for transforming one set of JS objects into another",
  "main": "dist/index.js",
  "scripts": {
    "test": "node test",
    "build": "npx babel-cli src/index.js > dist/index.js && npx babel-cli src/model.js > dist/model.js",
    "build-g": "babel src/index.js > dist/index.js && babel src/model.js > dist/model.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/victor-stone/serialize.git"
  },
  "author": "victor stone",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/victor-stone/serialize/issues"
  },
  "homepage": "https://github.com/victor-stone/serialize#readme",
  "babel": {
    "presets": [
      "env"
    ]
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1"
  }
}
